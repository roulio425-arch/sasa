<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Bot - Ultimate Server Enhancement</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-red: #ff0d0d;
            --dark-red: #8b0000;
            --blood-red: #660000;
            --crimson: #dc143c;
            --neon-red: #ff073a;
            --deep-black: #0a0a0a;
            --card-black: #111111;
            --border-red: #330000;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Rajdhani', sans-serif;
            background: linear-gradient(135deg, var(--deep-black) 0%, #1a0000 50%, var(--deep-black) 100%);
            color: #ffffff;
            overflow-x: hidden;
            cursor: none;
        }

        .custom-cursor {
            position: fixed;
            width: 20px;
            height: 20px;
            background: radial-gradient(circle, var(--neon-red) 0%, transparent 70%);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            transition: transform 0.1s ease;
            box-shadow: 0 0 20px var(--neon-red);
        }

        .cursor-trail {
            position: fixed;
            width: 6px;
            height: 6px;
            background: var(--primary-red);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9998;
            opacity: 0.8;
            animation: fadeTrail 0.5s ease-out forwards;
        }

        @keyframes fadeTrail {
            from { opacity: 0.8; transform: scale(1); }
            to { opacity: 0; transform: scale(0); }
        }

        .particles-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .particle {
            position: absolute;
            background: var(--neon-red);
            border-radius: 50%;
            opacity: 0.6;
            animation: float 6s infinite ease-in-out;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }

        .glitch {
            animation: glitch 2s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% { transform: translate(0); }
            10% { transform: translate(-2px, 2px); }
            20% { transform: translate(2px, -2px); }
            30% { transform: translate(-2px, -2px); }
            40% { transform: translate(2px, 2px); }
            50% { transform: translate(-2px, 2px); }
            60% { transform: translate(2px, -2px); }
            70% { transform: translate(-2px, -2px); }
            80% { transform: translate(2px, 2px); }
        }

        .neon-glow {
            text-shadow: 
                0 0 5px var(--neon-red),
                0 0 10px var(--neon-red),
                0 0 15px var(--neon-red),
                0 0 20px var(--neon-red);
            animation: pulse-glow 2s ease-in-out infinite alternate;
        }

        @keyframes pulse-glow {
            from { 
                text-shadow: 
                    0 0 5px var(--neon-red),
                    0 0 10px var(--neon-red),
                    0 0 15px var(--neon-red),
                    0 0 20px var(--neon-red);
            }
            to { 
                text-shadow: 
                    0 0 10px var(--neon-red),
                    0 0 20px var(--neon-red),
                    0 0 30px var(--neon-red),
                    0 0 40px var(--neon-red);
            }
        }

        .morphing-bg {
            background: linear-gradient(-45deg, var(--deep-black), var(--blood-red), var(--deep-black), var(--dark-red));
            background-size: 400% 400%;
            animation: morphing 15s ease infinite;
        }

        @keyframes morphing {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .card-3d {
            background: linear-gradient(145deg, var(--card-black), #1a1a1a);
            border: 2px solid var(--border-red);
            border-radius: 20px;
            padding: 2rem;
            position: relative;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            transform-style: preserve-3d;
            cursor: pointer;
        }

        .card-3d::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent, var(--neon-red), transparent);
            opacity: 0;
            transition: opacity 0.5s ease;
            z-index: -1;
        }

        .card-3d:hover {
            transform: translateY(-20px) rotateX(10deg) rotateY(5deg) scale(1.05);
            box-shadow: 
                0 30px 60px rgba(255, 7, 58, 0.3),
                0 0 40px rgba(255, 7, 58, 0.2),
                inset 0 0 20px rgba(255, 7, 58, 0.1);
            border-color: var(--neon-red);
        }

        .card-3d:hover::before {
            opacity: 0.1;
        }

        .card-3d:hover .card-icon {
            transform: rotateY(360deg) scale(1.3);
            color: var(--neon-red);
            text-shadow: 0 0 20px var(--neon-red);
        }

        .card-icon {
            font-size: 3rem;
            color: var(--primary-red);
            transition: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: block;
            margin-bottom: 1rem;
        }

        .floating {
            animation: floating 3s ease-in-out infinite;
        }

        @keyframes floating {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .rotating {
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .hero-logo {
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, var(--neon-red), var(--dark-red));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 5rem;
            color: white;
            margin: 0 auto 2rem;
            position: relative;
            overflow: hidden;
            animation: logo-pulse 2s ease-in-out infinite;
        }

        .hero-logo::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(from 0deg, transparent, var(--neon-red), transparent);
            animation: rotate 3s linear infinite;
        }

        .hero-logo::after {
            content: '';
            position: absolute;
            inset: 10px;
            background: radial-gradient(circle, var(--deep-black), var(--card-black));
            border-radius: 50%;
        }

        .hero-logo i {
            position: relative;
            z-index: 2;
        }

        @keyframes logo-pulse {
            0%, 100% { box-shadow: 0 0 20px var(--neon-red); }
            50% { box-shadow: 0 0 40px var(--neon-red), 0 0 60px var(--neon-red); }
        }

        .btn-enhanced {
            background: linear-gradient(45deg, var(--dark-red), var(--primary-red));
            border: none;
            padding: 1rem 2rem;
            border-radius: 10px;
            color: white;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn-enhanced::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .btn-enhanced:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 15px 30px rgba(255, 13, 13, 0.4);
        }

        .btn-enhanced:hover::before {
            left: 100%;
        }

        .team-card {
            background: linear-gradient(145deg, var(--card-black), #1a1a1a);
            border: 2px solid var(--border-red);
            border-radius: 20px;
            padding: 2rem;
            text-align: center;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .team-card::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, var(--neon-red), var(--primary-red), var(--dark-red), var(--neon-red));
            border-radius: 20px;
            z-index: -1;
            opacity: 0;
            transition: opacity 0.5s ease;
        }

        .team-card:hover {
            transform: translateY(-15px) rotateX(5deg);
            box-shadow: 0 25px 50px rgba(255, 7, 58, 0.3);
        }

        .team-card:hover::before {
            opacity: 1;
        }

        .team-avatar {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: linear-gradient(45deg, var(--primary-red), var(--neon-red));
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 3rem;
            color: white;
            transition: all 0.5s ease;
            position: relative;
        }

        .team-avatar::before {
            content: '';
            position: absolute;
            inset: 4px;
            background: var(--card-black);
            border-radius: 50%;
            z-index: -1;
        }

        .team-card:hover .team-avatar {
            transform: rotateY(360deg) scale(1.1);
            box-shadow: 0 0 30px var(--neon-red);
        }

        .faq-item {
            background: linear-gradient(145deg, var(--card-black), #1a1a1a);
            border: 2px solid var(--border-red);
            border-radius: 15px;
            margin-bottom: 1rem;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .faq-item:hover {
            border-color: var(--neon-red);
            box-shadow: 0 10px 30px rgba(255, 7, 58, 0.2);
        }

        .faq-question {
            padding: 1.5rem;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .faq-question:hover {
            color: var(--neon-red);
        }

        .faq-answer {
            padding: 0 1.5rem;
            max-height: 0;
            overflow: hidden;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .faq-answer.active {
            max-height: 200px;
            padding: 0 1.5rem 1.5rem;
        }

        .review-card {
            background: linear-gradient(145deg, var(--card-black), #1a1a1a);
            border: 2px solid var(--border-red);
            border-radius: 20px;
            padding: 2rem;
            margin: 0 1rem;
            position: relative;
            overflow: hidden;
            transition: all 0.5s ease;
        }

        .review-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 7, 58, 0.1), transparent);
            transition: left 1s ease;
        }

        .review-card:hover::before {
            left: 100%;
        }

        .review-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(255, 7, 58, 0.2);
            border-color: var(--neon-red);
        }

        .stars {
            color: var(--neon-red);
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }

        .section-title {
            font-family: 'Orbitron', monospace;
            font-size: 3rem;
            font-weight: 900;
            text-align: center;
            margin-bottom: 3rem;
            background: linear-gradient(45deg, var(--neon-red), var(--primary-red), var(--crimson));
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--neon-red), transparent);
        }

        @media (max-width: 768px) {
            .hero-logo {
                width: 150px;
                height: 150px;
                font-size: 4rem;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .card-3d {
                padding: 1.5rem;
            }
        }

        .scroll-indicator {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--border-red);
            z-index: 1000;
        }

        .scroll-progress {
            height: 100%;
            background: linear-gradient(90deg, var(--neon-red), var(--primary-red));
            width: 0%;
            transition: width 0.1s ease;
        }

        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--deep-black);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 10000;
            transition: opacity 0.5s ease;
        }

        .loader {
            width: 60px;
            height: 60px;
            border: 3px solid var(--border-red);
            border-top: 3px solid var(--neon-red);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="morphing-bg">
    <!-- Loading Screen -->
    <div class="loading-screen" id="loadingScreen">
        <div class="loader"></div>
    </div>

    <!-- Scroll Indicator -->
    <div class="scroll-indicator">
        <div class="scroll-progress" id="scrollProgress"></div>
    </div>

    <!-- Custom Cursor -->
    <div class="custom-cursor" id="cursor"></div>

    <!-- Particles Background -->
    <div class="particles-bg" id="particles"></div>

    <!-- Navigation -->
    <nav class="fixed top-0 w-full bg-black bg-opacity-50 backdrop-blur-md z-50 transition-all duration-300" id="navbar">
        <div class="container mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <div class="text-2xl font-bold neon-glow">
                    <i class="fab fa-discord"></i> UltimateBot
                </div>
                <div class="hidden md:flex space-x-6">
                    <a href="#features" class="hover:text-red-400 transition-colors">Features</a>
                    <a href="#team" class="hover:text-red-400 transition-colors">Team</a>
                    <a href="#reviews" class="hover:text-red-400 transition-colors">Reviews</a>
                    <a href="#faq" class="hover:text-red-400 transition-colors">FAQ</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="min-h-screen flex items-center justify-center relative">
        <div class="container mx-auto px-4 text-center z-10">
            <div class="hero-logo floating">
                <i class="fab fa-discord"></i>
            </div>
            <h1 class="text-6xl md:text-8xl font-black mb-6 glitch neon-glow" style="font-family: 'Orbitron', monospace;">
                ULTIMATE BOT
            </h1>
            <p class="text-xl md:text-2xl mb-8 text-gray-300 max-w-2xl mx-auto">
                Transform your Discord server with advanced moderation, entertainment, and utility features powered by cutting-edge AI technology.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                <button class="btn-enhanced">
                    <i class="fas fa-plus mr-2"></i> Invite Bot
                </button>
                <button class="btn-enhanced">
                    <i class="fas fa-headset mr-2"></i> Join Support
                </button>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="py-20 relative z-10">
        <div class="container mx-auto px-4">
            <h2 class="section-title">Bot Features</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <div class="card-3d">
                    <i class="fas fa-shield-alt card-icon"></i>
                    <h3 class="text-2xl font-bold mb-4 text-white">Advanced Moderation</h3>
                    <p class="text-gray-300">AI-powered moderation with smart auto-mod, custom filters, and comprehensive logging system.</p>
                </div>
                <div class="card-3d">
                    <i class="fas fa-gamepad card-icon"></i>
                    <h3 class="text-2xl font-bold mb-4 text-white">Entertainment Hub</h3>
                    <p class="text-gray-300">Mini-games, trivia, memes, music commands, and interactive entertainment features.</p>
                </div>
                <div class="card-3d">
                    <i class="fas fa-cogs card-icon"></i>
                    <h3 class="text-2xl font-bold mb-4 text-white">Server Management</h3>
                    <p class="text-gray-300">Role management, channel organization, automated welcome messages, and server statistics.</p>
                </div>
                <div class="card-3d">
                    <i class="fas fa-chart-line card-icon"></i>
                    <h3 class="text-2xl font-bold mb-4 text-white">Analytics & Insights</h3>
                    <p class="text-gray-300">Detailed server analytics, user activity tracking, and growth metrics visualization.</p>
                </div>
                <div class="card-3d">
                    <i class="fas fa-robot card-icon"></i>
                    <h3 class="text-2xl font-bold mb-4 text-white">AI Assistant</h3>
                    <p class="text-gray-300">Intelligent chatbot with natural language processing and context-aware responses.</p>
                </div>
                <div class="card-3d">
                    <i class="fas fa-puzzle-piece card-icon"></i>
                    <h3 class="text-2xl font-bold mb-4 text-white">Custom Commands</h3>
                    <p class="text-gray-300">Create custom commands, autoresponders, and personalized bot interactions for your server.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Team Section -->
    <section id="team" class="py-20 relative z-10">
        <div class="container mx-auto px-4">
            <h2 class="section-title">Our Team</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="team-card">
                    <div class="team-avatar">
                        <i class="fas fa-user-tie"></i>
                    </div>
                    <h3 class="text-xl font-bold mb-2">Alex Johnson</h3>
                    <p class="text-red-400 mb-3">Lead Developer</p>
                    <p class="text-gray-300 text-sm mb-4">Full-stack developer with 5+ years of Discord bot development experience.</p>
                    <div class="flex justify-center space-x-4">
                        <i class="fab fa-github text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                        <i class="fab fa-twitter text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                        <i class="fab fa-linkedin text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                    </div>
                </div>
                <div class="team-card">
                    <div class="team-avatar">
                        <i class="fas fa-user-cog"></i>
                    </div>
                    <h3 class="text-xl font-bold mb-2">Sarah Chen</h3>
                    <p class="text-red-400 mb-3">AI Specialist</p>
                    <p class="text-gray-300 text-sm mb-4">Machine learning engineer focusing on natural language processing and AI systems.</p>
                    <div class="flex justify-center space-x-4">
                        <i class="fab fa-github text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                        <i class="fab fa-twitter text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                        <i class="fab fa-linkedin text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                    </div>
                </div>
                <div class="team-card">
                    <div class="team-avatar">
                        <i class="fas fa-user-shield"></i>
                    </div>
                    <h3 class="text-xl font-bold mb-2">Mike Rodriguez</h3>
                    <p class="text-red-400 mb-3">Security Expert</p>
                    <p class="text-gray-300 text-sm mb-4">Cybersecurity specialist ensuring bot security and data protection standards.</p>
                    <div class="flex justify-center space-x-4">
                        <i class="fab fa-github text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                        <i class="fab fa-twitter text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                        <i class="fab fa-linkedin text-xl hover:text-red-400 cursor-pointer transition-colors"></i>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Reviews Section -->
    <section id="reviews" class="py-20 relative z-10">
        <div class="container mx-auto px-4">
            <h2 class="section-title">User Reviews</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <div class="review-card">
                    <div class="stars">
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                    </div>
                    <p class="text-gray-300 mb-4">"This bot completely transformed our server management. The AI moderation is incredibly accurate and saves us hours of work every day!"</p>
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-red-600 rounded-full flex items-center justify-center mr-3">
                            <i class="fas fa-user"></i>
                        </div>
                        <div>
                            <p class="font-semibold">GamerKing2023</p>
                            <p class="text-sm text-gray-400">Server Owner</p>
                        </div>
                    </div>
                </div>
                <div class="review-card">
                    <div class="stars">
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                    </div>
                    <p class="text-gray-300 mb-4">"The entertainment features are amazing! Our community loves the mini-games and the music commands work flawlessly."</p>
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-red-600 rounded-full flex items-center justify-center mr-3">
                            <i class="fas fa-user"></i>
                        </div>
                        <div>
                            <p class="font-semibold">CommunityBuilder</p>
                            <p class="text-sm text-gray-400">Community Manager</p>
                        </div>
                    </div>
                </div>
                <div class="review-card">
                    <div class="stars">
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                    </div>
                    <p class="text-gray-300 mb-4">"Best Discord bot I've ever used. The custom commands feature is incredibly powerful and the support team is very responsive."</p>
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-red-600 rounded-full flex items-center justify-center mr-3">
                            <i class="fas fa-user"></i>
                        </div>
                        <div>
                            <p class="font-semibold">TechModerator</p>
                            <p class="text-sm text-gray-400">Moderator</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- FAQ Section -->
    <section id="faq" class="py-20 relative z-10">
        <div class="container mx-auto px-4">
            <h2 class="section-title">Frequently Asked Questions</h2>
            <div class="max-w-3xl mx-auto">
                <div class="faq-item">
                    <div class="faq-question" onclick="toggleFAQ(0)">
                        <span>How do I invite the bot to my server?</span>
                        <i class="fas fa-chevron-down transition-transform duration-300" id="icon-0"></i>
                    </div>
                    <div class="faq-answer" id="answer-0">
                        <p class="text-gray-300">Click the "Invite Bot" button above and follow the authorization process. Make sure you have "Manage Server" permissions on your Discord server.</p>
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question" onclick="toggleFAQ(1)">
                        <span>Is the bot free to use?</span>
                        <i class="fas fa-chevron-down transition-transform duration-300" id="icon-1"></i>
                    </div>
                    <div class="faq-answer" id="answer-1">
                        <p class="text-gray-300">Yes! Our bot offers a comprehensive free tier with most features included. Premium features are available for advanced server management needs.</p>
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question" onclick="toggleFAQ(2)">
                        <span>How do I configure the moderation settings?</span>
                        <i class="fas fa-chevron-down transition-transform duration-300" id="icon-2"></i>
                    </div>
                    <div class="faq-answer" id="answer-2">
                        <p class="text-gray-300">Use the /config command to access the moderation panel. You can customize auto-mod rules, set punishment levels, and configure warning systems.</p>
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question" onclick="toggleFAQ(3)">
                        <span>What permissions does the bot need?</span>
                        <i class="fas fa-chevron-down transition-transform duration-300" id="icon-3"></i>
                    </div>
                    <div class="faq-answer" id="answer-3">
                        <p class="text-gray-300">The bot requires basic permissions like Send Messages, Read Message History, and Manage Messages for core functionality. Additional features may require specific permissions.</p>
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question" onclick="toggleFAQ(4)">
                        <span>How can I get support?</span>
                        <i class="fas fa-chevron-down transition-transform duration-300" id="icon-4"></i>
                    </div>
                    <div class="faq-answer" id="answer-4">
                        <p class="text-gray-300">Join our support server using the "Join Support" button above. Our team and community are ready to help with any questions or issues you may have.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Call to Action -->
    <section class="py-20 text-center relative z-10">
        <div class="container mx-auto px-4">
            <h2 class="text-4xl md:text-6xl font-black mb-6 neon-glow" style="font-family: 'Orbitron', monospace;">
                Ready to Transform Your Server?
            </h2>
            <p class="text-xl mb-8 text-gray-300 max-w-2xl mx-auto">
                Join thousands of servers already using UltimateBot to enhance their Discord experience.
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                <button class="btn-enhanced text-lg px-8 py-4">
                    <i class="fas fa-rocket mr-2"></i> Invite Bot Now
                </button>
                <button class="btn-enhanced text-lg px-8 py-4">
                    <i class="fas fa-discord mr-2"></i> Join Support Server
                </button>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-black bg-opacity-80 py-8 relative z-10">
        <div class="container mx-auto px-4 text-center">
            <div class="flex justify-center space-x-6 mb-4">
                <i class="fab fa-discord text-2xl hover:text-red-400 cursor-pointer transition-colors"></i>
                <i class="fab fa-twitter text-2xl hover:text-red-400 cursor-pointer transition-colors"></i>
                <i class="fab fa-github text-2xl hover:text-red-400 cursor-pointer transition-colors"></i>
                <i class="fab fa-youtube text-2xl hover:text-red-400 cursor-pointer transition-colors"></i>
            </div>
            <p class="text-gray-400">&copy; 2024 UltimateBot. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Custom cursor and trails
        const cursor = document.getElementById('cursor');
        const trailCount = 10;
        let trails = [];

        document.addEventListener('mousemove', (e) => {
            cursor.style.left = e.clientX - 10 + 'px';
            cursor.style.top = e.clientY - 10 + 'px';
            
            // Create trail effect
            const trail = document.createElement('div');
            trail.className = 'cursor-trail';
            trail.style.left = e.clientX - 3 + 'px';
            trail.style.top = e.clientY - 3 + 'px';
            document.body.appendChild(trail);
            
            trails.push(trail);
            if (trails.length > trailCount) {
                const oldTrail = trails.shift();
                if (oldTrail.parentNode) {
                    oldTrail.parentNode.removeChild(oldTrail);
                }
            }
            
            setTimeout(() => {
                if (trail.parentNode) {
                    trail.parentNode.removeChild(trail);
                }
            }, 500);
        });

        // Particles system
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 50;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                
                const size = Math.random() * 4 + 1;
                particle.style.width = size + 'px';
                particle.style.height = size + 'px';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 6 + 's';
                particle.style.animationDuration = (Math.random() * 4 + 4) + 's';
                
                particlesContainer.appendChild(particle);
            }
        }

        // FAQ Toggle
        function toggleFAQ(index) {
            const answer = document.getElementById(`answer-${index}`);
            const icon = document.getElementById(`icon-${index}`);
            
            answer.classList.toggle('active');
            icon.style.transform = answer.classList.contains('active') ? 'rotate(180deg)' : 'rotate(0deg)';
        }

        // Scroll progress
        function updateScrollProgress() {
            const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
            const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrollProgress = (scrollTop / scrollHeight) * 100;
            
            document.getElementById('scrollProgress').style.width = scrollProgress + '%';
        }

        // Smooth scrolling for navigation
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Navbar background on scroll
        function updateNavbar() {
            const navbar = document.getElementById('navbar');
            const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
            
            if (scrollTop > 50) {
                navbar.style.background = 'rgba(0, 0, 0, 0.9)';
                navbar.style.backdropFilter = 'blur(20px)';
            } else {
                navbar.style.background = 'rgba(0, 0, 0, 0.5)';
                navbar.style.backdropFilter = 'blur(10px)';
            }
        }

        // Intersection Observer for animations
        function setupIntersectionObserver() {
            const options = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, options);
            
            document.querySelectorAll('.card-3d, .team-card, .review-card').forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(50px)';
                card.style.transition = 'all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
                observer.observe(card);
            });
        }

        // Loading screen
        function hideLoadingScreen() {
            const loadingScreen = document.getElementById('loadingScreen');
            setTimeout(() => {
                loadingScreen.style.opacity = '0';
                setTimeout(() => {
                    loadingScreen.style.display = 'none';
                }, 500);
            }, 1000);
        }

        // Enhanced hover effects for buttons
        function setupButtonEffects() {
            document.querySelectorAll('.btn-enhanced').forEach(btn => {
                btn.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-3px) scale(1.05)';
                });
                
                btn.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0) scale(1)';
                });
            });
        }

        // Initialize everything
        document.addEventListener('DOMContentLoaded', function() {
            createParticles();
            setupIntersectionObserver();
            setupButtonEffects();
            hideLoadingScreen();
            
            // Event listeners
            window.addEventListener('scroll', () => {
                updateScrollProgress();
                updateNavbar();
            });
            
            // Initialize scroll-based effects
            updateScrollProgress();
            updateNavbar();
        });

        // Performance optimization
        let ticking = false;
        
        function requestTick() {
            if (!ticking) {
                requestAnimationFrame(() => {
                    updateScrollProgress();
                    updateNavbar();
                    ticking = false;
                });
                ticking = true;
            }
        }
        
        window.addEventListener('scroll', requestTick);
    </script>
</body>
</html>
